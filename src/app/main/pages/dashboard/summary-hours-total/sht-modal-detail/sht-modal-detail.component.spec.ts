import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ShtModalDetailComponent } from './sht-modal-detail.component';

describe('ShtModalDetailComponent', () => {
  /*let component: ShtModalDetailComponent;
  let fixture: ComponentFixture<ShtModalDetailComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ ShtModalDetailComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(ShtModalDetailComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  }); */
});
